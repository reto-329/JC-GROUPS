<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/canonical') %>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= equipment.description ? equipment.description : 'Rent equipment from JC Equipment Rentals' %>">
  <title><%= title %></title>
  <link rel="icon" href="/images/Favicon.png">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/index.css">
  <link rel="stylesheet" href="/css/header.css">
  <link rel="stylesheet" href="/css/footer.css">
  <link rel="stylesheet" href="/css/rental-details.css">
</head>
<body>

  <%- include('partials/header') %>

  <main
    class="rental-container"
    data-equipment-id="<%= equipment._id %>"
    data-daily-rate="<%= equipment.dailyRate %>"
    data-qty-available="<%= equipment.quantityAvailable %>"
    data-user-id="<%= userId || '' %>"
    data-equipment-name="<%= equipment.name %>"
    data-equipment-image="<%= equipment.image || '' %>"
    data-service-areas='<%- JSON.stringify(serviceAreas || []) %>'
  >
    <!-- Equipment Details -->
    <div class="rd-details">
      <div class="rd-image">
        <% if (equipment.image) { %>
          <img src="<%= equipment.image %>" alt="<%= equipment.name %>">
        <% } else { %>
          <i class="fas fa-tools"></i>
        <% } %>
      </div>

      <div class="rd-info">
        <h2><%= equipment.name %></h2>
        <p><strong>Category:</strong> <%= equipment.category %></p>
        <p><strong>Description:</strong> <%= equipment.description %></p>
        <% if (equipment.manufacturer) { %><p><strong>Manufacturer:</strong> <%= equipment.manufacturer %></p><% } %>
        <% if (equipment.model) { %><p><strong>Model:</strong> <%= equipment.model %></p><% } %>
        <p><strong>Available:</strong> <%= equipment.quantityAvailable %> of <%= equipment.quantity %></p>

        <!-- Specifications Table -->
        <% if (equipment.specifications && Object.keys(equipment.specifications || {}).length > 0) { %>
          <div class="rd-specifications" style="margin-top: 20px;">
            <h3 style="margin: 0 0 12px 0; font-size: 1.1rem; color: #333; font-weight: 600;">Specifications</h3>
            <table class="rd-specs-table" style="width: 100%; border-collapse: collapse;">
              <thead>
                <tr style="background: #f5f5f5; border-bottom: 2px solid #ddd;">
                  <th style="padding: 10px; text-align: left; font-weight: 600; color: #555; width: 40%;">Name</th>
                  <th style="padding: 10px; text-align: left; font-weight: 600; color: #555;">Value</th>
                </tr>
              </thead>
              <tbody>
                <% Object.entries(equipment.specifications).forEach(([key, value]) => { %>
                  <tr style="border-bottom: 1px solid #eee;">
                    <td style="padding: 10px; color: #666;"><strong><%= key %></strong></td>
                    <td style="padding: 10px; color: #333;"><%= value %></td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        <% } %>
      </div>

      <div class="rd-pricing-box">
        <div class="rd-pricing-option">
          <div class="rd-pricing-label">DAILY</div>
          <div class="rd-pricing-price">$<%= equipment.dailyRate %></div>
        </div>
        <div class="rd-pricing-option">
          <div class="rd-pricing-label">WEEKLY</div>
          <div class="rd-pricing-price">$<%= equipment.weeklyRate || (equipment.dailyRate * 7 * 0.9).toFixed(2) %></div>
        </div>
        <div class="rd-pricing-option">
          <div class="rd-pricing-label">MONTHLY</div>
          <div class="rd-pricing-price">$<%= equipment.monthlyRate || (equipment.dailyRate * 30 * 0.8).toFixed(2) %></div>
        </div>
      </div>
    </div>

    <!-- Rental Form -->
    <div class="rd-form-wrap">
      <form id="rentalForm">
        <!-- Step 1: Postal Code -->
        <div class="rd-step">
          <div class="rd-step-header">
            <div class="rd-step-number">1</div>
            <div class="rd-step-label">Delivery Location</div>
          </div>
          <div class="rd-form-group">
            <label for="postalCode">Select Service Area</label>
            <select id="postalCode" name="postalCode" required>
              <option value="">-- Choose a delivery area --</option>
              <% if (serviceAreas && serviceAreas.length > 0) { %>
                <% serviceAreas.forEach(area => { %>
                  <option value="<%= area.postalCode %>">
                    <%= area.postalCode %> (<%= area.city %>, <%= area.province %>) - Delivery: $<%= area.deliveryFee.toFixed(2) %>
                  </option>
                <% }); %>
              <% } else { %>
                <option value="" disabled>No service areas available</option>
              <% } %>
            </select>
            <div id="postalCodeError" class="rd-field-error"></div>
          </div>
        </div>

        <!-- Step 2: Rental Duration -->
        <div class="rd-step">
          <div class="rd-step-header">
            <div class="rd-step-number">2</div>
            <div class="rd-step-label">Rental Duration</div>
          </div>
          <div class="rd-form-group">
            <label for="rentalDays">Number of days</label>
            <div class="rd-input-row">
              <input type="number" id="rentalDays" name="rentalDays" value="1" min="1" required>
              <span class="rd-input-suffix">Day(s)</span>
            </div>
          </div>
        </div>

        <!-- Step 3: Quantity -->
        <div class="rd-step">
          <div class="rd-step-header">
            <div class="rd-step-number">3</div>
            <div class="rd-step-label">Quantity</div>
          </div>
          <div class="rd-form-group">
            <label for="rentalQty">Units to rent</label>
            <div class="rd-qty-control">
              <button type="button" id="decreaseQty">−</button>
              <input type="number" id="rentalQty" name="quantity" value="1" min="1" max="<%= equipment.quantityAvailable %>">
              <button type="button" id="increaseQty">+</button>
            </div>
          </div>
        </div>

        <!-- Step 4: Start Date -->
        <div class="rd-step">
          <div class="rd-step-header">
            <div class="rd-step-number">4</div>
            <div class="rd-step-label">Pickup Date</div>
          </div>
          <div class="rd-form-group">
            <label for="startDate">Start Date</label>
            <input type="date" id="startDate" name="startDate" required>
          </div>
        </div>

        <!-- Step 5: Summary -->
        <div class="rd-step">
          <div class="rd-step-header">
            <div class="rd-step-number">5</div>
            <div class="rd-step-label">Order Summary</div>
          </div>

          <div class="rd-summary">
            <div class="rd-summary-row">
              <span><span id="summaryDays">1</span> day(s) × <span id="summaryQty">1</span> unit(s)</span>
              <span>$<span id="equipmentCost">0.00</span></span>
            </div>
            <div class="rd-summary-row rd-summary-total">
              <span>TOTAL</span>
              <span>$<span id="totalPrice">0.00</span></span>
            </div>
          </div>

          <button type="button" id="completeOrderBtn" class="rd-submit-btn">
            <i class="fas fa-shopping-cart"></i> COMPLETE ORDER
          </button>
          <a href="tel:6133342571" class="rd-call-btn">
            <i class="fas fa-phone"></i> CALL NOW TO RENT
          </a>
        </div>
      </form>
    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="/js/rental-details.js"></script>

</body>
</html>
